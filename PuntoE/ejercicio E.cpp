#include <iostream>
#include <stdio.h>
using namespace std;

int main(int argc, char *argv[]) {
	
	int area, lado;
	
	
		while (lado ! = 0)
		{
			area = (lado * lado) * 6;
			printf(“El area del cubo es: %d”, area);
			printf (ingrese el lado de otro cubo\n);
			scanf (“%d”, &lado);
		}
	
	return 0;
}

